# include <stdio.h>
# include <stdlib.h>
# include <string.h>
# include <dir.h>
# include <dos.h>
# include <conio.h>
# include <ctype.h>

unsigned int  test, SBAdr, MixSpeed;
unsigned char SBIRQ, SBDMA;
union REGS Regs; //Interrupt Register

char drives[26][27];
int anzlw, lfw_neu, lfw_start;
char fname[200][27];
char far file_to_load[80];
char s3m_in_memory=0;

char far *FileName; //FileName Pointer

int file_selected, eintrag_oben, eintrag_akt, max_eintrag;
int max;
int akt_drive;
char pfad[100];
char startpath[200],aktpath[200];
int alength=200;
int status;
char ext[15];
char dest[20], *dest2;
int  sy_old, sy;
long *dostime, oldtime;

// ** Daten zur Soundblastereinstellung **
char SBI10[3][6]={"220","240","260"};
char SBI20[4][6]={"1","3","5","7"};
char SBI30[4][6]={"1","2","4","5"};
char SBI40[4][6]={" 8000","20000","30000","44100"};

//* Externe ASSEMBLER Routinen *
extern unsigned char InitFMS (void);
extern unsigned char LoadS3MF (char far *FileName);
extern void          CleanS3M (void);
extern unsigned char InitPlay (void);
extern void          ResetPlay (void);
extern void          ClosePlay (void);
extern unsigned int  InitSB (int BasisIO, char IRQ, char DMA , unsigned int MixSpeed);
extern unsigned int  ResetSB (void);
extern void          StartPlay (void);
extern void          StopPlay (void);
extern void          InitMixer (void);
extern void          StartMixer (void);
extern void          StopMixer (void);
extern void          CallMixer (void);

extern void putchr(int x,int y,int zeichen,char attrib);
extern void putstring(int x,int y,void *string,char attrib);
extern void _fputstring(int x,int y,void far *string,char attrib);

#include "fx.h"
#include "tasten.h"
#include "ini.c"
#include "screen.c"
#include "modp.c"
#include "filedsk3.c"

//****************************************************************


int main(void)
{
  int check;

check = FMS_Install();
      if (check == 255) return 0;     

//*** VGA-TextModus 80x43/50 ***

NewScreen();
cursor(unsichtbar);

build_screen();

read_ini();

check = sb_check();
   if (check == 255) return 0;   // Fehler aufgetreten

   check = Play_Install();
   if (check == 255) return 0;


   main_loop();

   if (s3m_in_memory == 1)
   {
   StopMixer();
   StopPlay();
   CleanS3M();
   }

   ClosePlay();    //R„umt Playervariablen auf und gibt PlayerMem frei

   setdisk(lfw_start);
   chdir(startpath);
   cursor(strich);

return 0;  // AUS IS !!!
}
